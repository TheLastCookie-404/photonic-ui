name: Publish to npm
run-name: Publishing to NPM 🚀
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4
      - run: ln -s ./src/components/ ./public/ui
